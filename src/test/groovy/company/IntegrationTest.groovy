package company

/**
 * Used for junit @Category so that we can include/exclude integration tests when running gradle tasks. For the
 * purposes of this class, and integration test is one that fires up a spring context.
 */
class IntegrationTest {
}
